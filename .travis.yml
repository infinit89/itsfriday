language: php
php:
  - 5.5
  - 5.6
  - 7.0
  - hhvm
  
services:
  - mongodb

before_install:
  - chmod +x rest/tests/travis/mongodb-install.sh
  
install:
  - rest/tests/travis/mongodb-install.sh

before_script:  
    - cd rest && composer update

script: phpunit rest/tests
